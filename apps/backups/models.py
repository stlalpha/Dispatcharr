# Simplified backup models - no complex job tracking needed
# Backups are handled directly via API calls, similar to Mealie

# Note: The BackupJob model has been removed in favor of a simpler approach.
# Backups are now simple file operations without job state tracking.
